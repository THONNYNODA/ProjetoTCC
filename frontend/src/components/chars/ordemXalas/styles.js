import { makeStyles } from "@material-ui/core/styles";

export const charStyles = makeStyles({
  teste: {
    width: '100%',
    height: '100%',
    fontSize:10,
    wordBreak:'normal'

  },
  
  
});
